{
  "tutorial": {
    "id": 2658,
    "version": 7,
    "type": "guidedProject",
    "isNew": false,
    "codeLanguage": "python",
    "title": "Bot - Part 2",
    "descriptionContent": "In part 2, you'll make use of the input function to make the bot interactive and let your users talk to it",
    "shortDescriptionContent": "",
    "showInTrack": true,
    "iconBanner": "tutorial-2658-icon-banner.svg",
    "chapters": [
      {
        "id": 14632,
        "title": "Bot - Part 2",
        "type": "practiceLevel3",
        "lessons": [
          {
            "title": "Lesson #1",
            "id": 129424,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Welcome back to the **Bot** project. In part 2, we'll modify the existing code by accepting user input to make it more interactive.\n\nLet's start with the first question.\n\n1. Remove the line where you declare the `name` variable and assign it a value.\n2. Add `input` function to capture the user's input and add `\"Hello! What is your name? \"` as the argument between the parentheses.\n\nRun the program and enter your name when prompted in the console. You'll see the program stops and waits for the user's input when using the `input` function.\n\nAlso, note that we can leave a space in the prompt used in the `input` function to make it easier for our user to type in their name, as we then have a whitespace between the question mark and the start of the user's text.",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "print(\"Hello! What is your name?\")\nname = \"John\"\nprint(f\"Nice to meet you, {name}!\")\nprint(\"How old are you?\")\nage = 25\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\nprint(\"What's your favorite color?\")\ncolor = \"blue\"\nprint(f\"Oh, {color} is a beautiful color!\")",
                  "solvedContent": "print(\"Hello! What is your name?\")\nname = input(\"Hello! What is your name? \")\nprint(f\"Nice to meet you, {name}!\")\nprint(\"How old are you?\")\nage = 25\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\nprint(\"What's your favorite color?\")\ncolor = \"blue\"\nprint(f\"Oh, {color} is a beautiful color!\")"
                }
              ],
              "hints": []
            }
          },
          {
            "title": "Lesson #2",
            "id": 129419,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Our bot now asks the user twice what their name is. Once because of the first `print` statement and once because of the newly added `input` function.\n\n1. Remove the `print` statement to display the question only once (using the `input` function instead of `print`.)",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "print(\"Hello! What is your name?\")\nname = input(\"Hello! What is your name? \")\nprint(f\"Nice to meet you, {name}!\")\nprint(\"How old are you?\")\nage = 25\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\nprint(\"What's your favorite color?\")\ncolor = \"blue\"\nprint(f\"Oh, {color} is a beautiful color!\")",
                  "solvedContent": "name = input(\"Hello! What is your name? \")\nprint(f\"Nice to meet you, {name}!\")\nprint(\"How old are you?\")\nage = 25\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\nprint(\"What's your favorite color?\")\ncolor = \"blue\"\nprint(f\"Oh, {color} is a beautiful color!\")"
                }
              ],
              "hints": []
            }
          },
          {
            "title": "Lesson #3",
            "id": 129420,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Continuing from there, let's make the user type in their age instead of defining it in the code itself.\n\n1. Replace the line where we define the `age` variable with the `input` function and use `\"How old are you? \"` with the `input` function and save the received input in a variable called `age_input`.\n2. Create a variable called `age` and use the `int` function to transform the input into a number like `age = int(age_input)`.\n3. Remove the `print` function displaying `\"How old are you? \"` to not ask twice.",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "name = input(\"Hello! What is your name? \")\nprint(f\"Nice to meet you, {name}!\")\nprint(\"How old are you?\")\nage = 25\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\nprint(\"What's your favorite color?\")\ncolor = \"blue\"\nprint(f\"Oh, {color} is a beautiful color!\")",
                  "solvedContent": "name = input(\"Hello! What is your name? \")\nprint(f\"Nice to meet you, {name}!\")\nage_input = input(\"How old are you? \")\nage = int(age_input)\n\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\nprint(\"What's your favorite color?\")\ncolor = \"blue\"\nprint(f\"Oh, {color} is a beautiful color!\")"
                }
              ],
              "hints": []
            }
          },
          {
            "title": "Lesson #4",
            "id": 129422,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Finally, let the user share their favorite color instead of pre-filling it.\n\n1. Replace the line where `color` is defined with an `input` function to ask the user `\"What's your favorite color? \"`. Save the result from the `input` function to a variable called `color`.\n2. Remove the `print` statement that asks for the color as the `input` statement should display the question.",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "name = input(\"Hello! What is your name? \")\nprint(f\"Nice to meet you, {name}!\")\nage_input = input(\"How old are you? \")\nage = int(age_input)\n\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\nprint(\"What's your favorite color?\")\ncolor = \"blue\"\nprint(f\"Oh, {color} is a beautiful color!\")",
                  "solvedContent": "name = input(\"Hello! What is your name? \")\nprint(f\"Nice to meet you, {name}!\")\nage_input = input(\"How old are you? \")\nage = int(age_input)\n\nbot_age = 3\nage_difference = age - bot_age\nprint(f\"You are {age_difference} years older than me. I'm only {bot_age} years old!\")\ncolor = input(\"What's your favorite color? \")\nprint(f\"Oh, {color} is a beautiful color!\")"
                }
              ],
              "hints": []
            }
          }
        ]
      }
    ]
  }
}