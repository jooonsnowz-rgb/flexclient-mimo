{
  "tutorial": {
    "id": 2399,
    "version": 7,
    "type": "guidedProjectOptional",
    "isNew": false,
    "codeLanguage": "python",
    "title": "Menganalisis Transaksi Bank",
    "descriptionContent": "Menganalisis Data Transaksi Bank dengan List Comprehension Python",
    "shortDescriptionContent": "",
    "showInTrack": true,
    "iconBanner": "tutorial-2399-icon-banner.svg",
    "chapters": [
      {
        "id": 13895,
        "title": "Menganalisis Transaksi Bank",
        "type": "practiceLevel1",
        "lessons": [
          {
            "title": "Starter code",
            "id": 123736,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Mari gunakan pengetahuanmu tentang list comprehension dan notasi slice untuk menganalisis daftar setoran dan penarikan dari rekening bank!\n\nPertama, mari pahami kode awal kita. Setiap transaksi dalam list `transactions_in_cents` dinyatakan dalam sen AS. List tersebut berisi transaksi positif (setoran) dan transaksi negatif (penarikan).\n\n1. Mari mulai dengan mencetak teks \"Transactions (cents):\" ke konsol.\n2. Lalu, cetak nilai variabel `transactions_in_cents` ke konsol.",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "We can use the built-in Python `print()` function as follows: \n- `print(variable_to_print)` or\n- `print(\"text to print\")`\n\nNote: you can use f-strings within calls to the `print()` function, too!"
                }
              ]
            }
          },
          {
            "title": "Converting to dollars",
            "id": 123660,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Selanjutnya, kita akan mengonversi transaksi dari sen ke dolar agar data yang disertakan lebih mudah dipahami.\n\n1. Buat variabel bernama `transactions`.\n2. Di dalam variabel ini, gunakan list comprehension untuk membagi setiap transaksi dari `transactions_in_cents` dengan 100.\n3. (Opsional) Pertimbangkan untuk mencetak nilai variabel `transactions` baru kamu bersama label untuk memverifikasi bahwa kode kamu berfungsi seperti yang diharapkan:\n```python\nprint(\"Transactions (dollars):\")\nprint(transactions)\n```",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "1. To create a basic list comprehension, we use the following syntax: `[x for x in list_name]`.\n2. To alter the elements in the newly-created list, specify the desired transformation within the list comprehension, like this: `[x * 5 for x in list_name]` (this example would generate a new list with each element from the original list multiplied by 5).\n3. For our code, we'll use `[transaction / 100 for transaction in transactions_in_cents]` to generate the new list. \n4. Be sure to store this code within a variable called `transactions`!"
                }
              ]
            }
          },
          {
            "title": "Sorting in ascending order",
            "id": 123663,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Selanjutnya, kita akan mengurutkan transaksi dalam urutan menaik (dari yang terkecil ke terbesar).\n\n1. Panggil method list bawaan `.sort()` pada list `transactions`.\n2. Kamu tidak perlu membuat variabel baru untuk menyimpannya; `.sort()` mengubah list yang sudah ada untuk kita!\n3. (Opsional) Pertimbangkan untuk mencetak nilai variabel `transactions` yang telah diperbarui bersama label guna memverifikasi bahwa kode kamu berfungsi sesuai harapan:\n```python\nprint(\"Sorted Transactions:\")\nprint(transactions)\n```",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\n\n\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\n"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "To sort a list in ascending order, we use this syntax: `list_name.sort()`\n\nBonus: since ascending is the default sort order, we don't need to pass any additional arguments into the `.sort()` method!"
                }
              ]
            }
          },
          {
            "title": "Filtering into deposits and withdrawals",
            "id": 123664,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Sekarang, mari gunakan list comprehension untuk membuat list baru yang difilter agar mencakup deposit (hanya angka positif!) dan penarikan (hanya angka negatif!)\n\n1. Buat dua variabel baru, `deposits` dan `withdrawals`.\n2. Di dalam `deposits`, tulis list comprehension untuk membuat list transaksi yang difilter dengan nilai lebih besar dari atau sama dengan 0.\n3. Di dalam `withdrawals`, tulis list comprehension untuk membuat list transaksi yang difilter dengan nilai kurang dari 0.\n4. (Opsional) Pertimbangkan untuk mencetak nilai variabel `deposits` dan `withdrawals` barumu beserta labelnya untuk memverifikasi bahwa kode berfungsi sesuai harapan:\n```python\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n```",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\n\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "To generate a filtered list with a list comprehension, you'll use an if statement after the for portion of your list comprehension: `x for x in list_name if condition_here`."
                }
              ]
            }
          },
          {
            "title": "Creating `invertNegativeNum` function",
            "id": 123665,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Sekarang kita akan membuat fungsi yang bisa kita gunakan untuk mengubah angka negatif (penarikan) menjadi nilai positifnya. Fungsi ini, yang diberi nama `invert_negative_num`, akan menerima sebuah angka dan mengembalikan hasil perkalian angka input tersebut dengan -1.\n\n1. Definisikan fungsi bernama `invert_negative_num`.\n2. Berikan fungsi kamu parameter bernama `negative_num`.\n3. Di dalam fungsi kamu, buat variabel baru `positive_num`.\n4. Di dalam variabel ini, kalikan `negative_num` dengan `-1`.\n5. Sebagai baris terakhir fungsi kamu, kembalikan `positive_num`.`",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "Creating a function with a return value follows this format:\n```python\ndef function_name(param_1, param_2):\n    # code any function logic in this nested block\n    # reminder: parameters are optionally used to represent values passed into the function call\n    return return_value\n```"
                }
              ]
            }
          },
          {
            "title": "Converting withdrawals to positives",
            "id": 123666,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Sekarang mari gunakan fungsi `invertNegativeNum` dengan mengonversi setiap jumlah dalam list `withdrawals` menjadi angka positif sekaligus!\n\n1. Atur variabel `withdrawals` yang sudah ada agar sama dengan list comprehension baru.\n2. Di dalam list comprehension ini, panggil fungsi `invert_negative_number` untuk setiap withdrawal yang ada.\n3. (Opsional) Pertimbangkan untuk mencetak nilai terbaru variabel `withdrawals` bersama label untuk memverifikasi bahwa kode kamu berfungsi sesuai harapan:\n```python\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n```",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "Use this code inside the list comprehension's square brackets:\n```Python\ninvert_negative_num(withdrawal) for withdrawal in withdrawals\n``` "
                }
              ]
            }
          },
          {
            "title": "Determining largest  withdrawals",
            "id": 123667,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Untuk membantu kita memahami kebiasaan pengeluaran dengan lebih baik (dan mendapatkan lebih banyak latihan menggunakan notasi slice list), mari tulis kode untuk membuat list baru yang berisi lima penarikan terbesar kita.\n\n1. Buat variabel baru bernama `largest_withdrawals`.\n2. Di dalam variabel ini, gunakan notasi slice list untuk membuat list baru yang berisi lima elemen pertama dari list `withdrawals`.\n3. Kamu tidak perlu mengurutkan `withdrawals` terlebih dahulu karena ternyata list tersebut sudah diurutkan dengan penarikan terbesar di urutan pertama!\n4. (Opsional) Pertimbangkan untuk mencetak nilai variabel `largest_withdrawals` bersama label guna memverifikasi bahwa kode kamu berfungsi sesuai harapan:\n```python\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n```",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\nlargest_withdrawals = withdrawals[:5]\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "1. Remember the syntax of list slicing: `list_name[start:stop:step]`\n2. You'll start by specifying the list you wish to slice from (`withdrawals`), followed by square brackets.\n3. Inside the square brackets, the first number specifies the index to **start** the slice at; this value can also be omitted if you wish to start at the beginning of the list. Don't forget Python indexing starts at 0!\n4. After the first colon, the second number corresponds to the index you wish to **stop** at. Remember that the value included at the **stop** index is *not* included in the slice.\n5. In this case, we don't need to specify **step**, as we want our slice to contain five consecutive withdrawal amounts.\n6. That gives us two ways to slice it: `withdrawals[:5]` or `withdrawals[0:5]`!"
                }
              ]
            }
          },
          {
            "title": "Averaging deposits and withdrawals",
            "id": 123668,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Untuk lebih memahami pola keuangan kita, mari hitung jumlah rata-rata deposit dan penarikan kita. Kita akan memulai dengan rata-rata ini sebagai angka desimal floating-point dan mengonversinya menjadi integer.\n\n1. Buat dua variabel baru, `average_deposit` dan `average_withdrawal`.\n2. Di dalam setiap variabel baru, ambil jumlah total transaksi dari list tersebut dan bagi dengan panjang list.\n3. Sekarang, gunakan variabel yang sudah ada dan gunakan type casting untuk mengonversi kedua rata-rata tersebut dari tipe `float` menjadi tipe `int`.\n4. (Opsional) Pertimbangkan untuk mencetak nilai variabel `average_deposit` dan `average_withdrawal` yang baru kamu buat, beserta labelnya, untuk memverifikasi bahwa kode kamu berfungsi sesuai harapan:\n```python\nprint(\"Average Deposit (int):\")\nprint(average_deposit)\nprint(\"Average Withdrawal (int):\")\nprint(average_withdrawal)\n```",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\nlargest_withdrawals = withdrawals[:5]\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\nlargest_withdrawals = withdrawals[:5]\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n\naverage_deposit = sum(deposits) / len(deposits)\naverage_withdrawal = sum(withdrawals) / len(withdrawals)\n\naverage_deposit = int(average_deposit)\naverage_withdrawal = int(average_withdrawal)\n\nprint(\"Average Deposit (int):\")\nprint(average_deposit)\nprint(\"Average Withdrawal (int):\")\nprint(average_withdrawal)"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "1. To calculate the value of `average_deposit`, use the following formula: `sum(deposits) / len(deposits)`.\n2. Then, to convert this value from a floating-point decimal into an integer, you can use type casting: `average_deposit = int(average_deposit)`. \n3. Reminder that casting a decimal to an integer does not round the number up or down, but instead just gives us the integer value to the left of the decimal point, omitting any decimal digits."
                }
              ]
            }
          },
          {
            "title": "Calculating account balance",
            "id": 123669,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Satu hal terakhir: mari kita mengetahui saldo akun kita saat ini dengan menjumlahkan semua nilai dalam array `transactions`!\n\n1. Buat variabel baru bernama `balance`.\n2. Di dalam variabel baru ini, hitung jumlah dari list `transactions` untuk mengetahui berapa banyak uang yang bisa kita belanjakan!\n3. (Opsional) Pertimbangkan untuk mencetak nilai variabel `balance` beserta label untuk memverifikasi bahwa kode kamu berjalan sesuai harapan:\n```python\nprint(\"Balance:\")\nprint(balance)\n```",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\nlargest_withdrawals = withdrawals[:5]\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n\naverage_deposit = sum(deposits) / len(deposits)\naverage_withdrawal = sum(withdrawals) / len(withdrawals)\n\naverage_deposit = int(average_deposit)\naverage_withdrawal = int(average_withdrawal)\n\nprint(\"Average Deposit (int):\")\nprint(average_deposit)\nprint(\"Average Withdrawal (int):\")\nprint(average_withdrawal)\n\n",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\nlargest_withdrawals = withdrawals[:5]\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n\naverage_deposit = sum(deposits) / len(deposits)\naverage_withdrawal = sum(withdrawals) / len(withdrawals)\n\naverage_deposit = int(average_deposit)\naverage_withdrawal = int(average_withdrawal)\n\nprint(\"Average Deposit (int):\")\nprint(average_deposit)\nprint(\"Average Withdrawal (int):\")\nprint(average_withdrawal)\n\nbalance = sum(transactions)\nprint(\"Balance:\")\nprint(balance)"
                }
              ],
              "hints": [
                {
                  "title": "Hint",
                  "content": "Set a new variable `balance` equal to `sum(transactions)`."
                }
              ]
            }
          },
          {
            "title": "Project recap",
            "id": 123735,
            "contentType": "executableFiles",
            "images": [],
            "executableContent": {
              "instructions": "Kerja bagus menggunakan keterampilan Python kamu yang terus berkembang untuk mengatur dan menganalisis transaksi bank!\n\nSelama proyek ini, kamu mengonversi transaksi dari sen ke dolar, mengurutkannya, membuat daftar baru berisi setoran, penarikan, dan penarikan terbesar, serta menghitung rata-rata transaksi dan saldo akhir rekening. Dalam prosesnya, kamu menggunakan list comprehension untuk membuat daftar yang dimodifikasi dan difilter, serta berlatih menggunakan type casting dan notasi slice.\n\nSekarang kesempatanmu untuk bereksperimen dengan kode proyek yang sudah selesai. Coba satu atau beberapa hal berikut:\n- Hitung jumlah total transaksi\n- Buat daftar baru untuk melacak saldo berjalan setelah setiap transaksi\n- Tulis fungsi untuk mencetak semua transaksi di antara nilai `min` dan `max` yang diberikan.",
              "preselectedFileIndex": 0,
              "hasVisualOutput": false,
              "files": [
                {
                  "name": "script.py",
                  "codeLanguage": "python",
                  "content": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\nlargest_withdrawals = withdrawals[:5]\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n\naverage_deposit = sum(deposits) / len(deposits)\naverage_withdrawal = sum(withdrawals) / len(withdrawals)\n\naverage_deposit = int(average_deposit)\naverage_withdrawal = int(average_withdrawal)\n\nprint(\"Average Deposit (int):\")\nprint(average_deposit)\nprint(\"Average Withdrawal (int):\")\nprint(average_withdrawal)\n\nbalance = sum(transactions)\nprint(\"Balance:\")\nprint(balance)",
                  "solvedContent": "transactions_in_cents = [1000, -450, 7500, -3475, 13000, -8150, 25000, -850, 0, -1125, -600, 102550, -25025, 6500, -3850, -2500, 1875, 4000]\nprint(\"Transactions (cents):\")\nprint(transactions_in_cents)\n\ntransactions = [transaction / 100 for transaction in transactions_in_cents]\nprint(\"Transactions (dollars):\")\nprint(transactions)\n\ntransactions.sort()\nprint(\"Sorted Transactions:\")\nprint(transactions)\n\ndeposits = [transaction for transaction in transactions if transaction >= 0]\nwithdrawals = [transaction for transaction in transactions if transaction < 0]\n\nprint(\"Deposits:\")\nprint(deposits)\nprint(\"Withdrawals:\")\nprint(withdrawals)\n\ndef invert_negative_num(negative_num):\n    positive_num = negative_num * -1\n    return positive_num\n\nwithdrawals = [invert_negative_num(withdrawal) for withdrawal in withdrawals]\nprint(\"Withdrawals (positive values):\")\nprint(withdrawals)\n\nlargest_withdrawals = withdrawals[:5]\nprint(\"Largest Withdrawals:\")\nprint(largest_withdrawals)\n\naverage_deposit = sum(deposits) / len(deposits)\naverage_withdrawal = sum(withdrawals) / len(withdrawals)\n\naverage_deposit = int(average_deposit)\naverage_withdrawal = int(average_withdrawal)\n\nprint(\"Average Deposit (int):\")\nprint(average_deposit)\nprint(\"Average Withdrawal (int):\")\nprint(average_withdrawal)\n\nbalance = sum(transactions)\nprint(\"Balance:\")\nprint(balance)"
                }
              ],
              "hints": []
            }
          }
        ]
      }
    ]
  }
}